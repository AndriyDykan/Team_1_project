[tool.poetry]
name = "team-1-project"
version = "0.1.0"
description = ""
authors = ["CatCaptures_team"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
